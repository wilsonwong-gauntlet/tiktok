1. **Firebase Web SDK**  
     ```ts
     import { initializeApp } from 'firebase/app'
     ```

2. **Video Playback**  
   - Use `expo-video` instead of `expo-av`.  
   - “[expo-av]: Video component from `expo-av` is deprecated in favor of `expo-video`.”  
   - Reference [expo-video docs](https://docs.expo.dev/versions/latest/sdk/video/) for the new API.